<div class="edit-container container">
    <div class="title-container">
        <div class="title-text">
            <h1>ABOLITION TO PINEAPPLE ON PIZZA!</h1>
            <p>Pineapple is wonderful, the problem is when it is on pizza</p>
        </div>
        <a href="/petition/logout" class="logout">LOGOUT</a>
    </div>
    
    <div class="flex-container">
        <div class="content-container">
            <h3>Edit your profile</h3>

            {{#userFullProfile}}
            <p class="error-message">{{../error}}</p>

            <form method="POST" class="edit-form">
                <label for="first">First name<span class="mandatory">*</span></label>
                <input placeholder="Type here" type="text" name="first" value="{{first}}" >

                <label for="last">Last name<span class="mandatory">*</span></label>
                <input placeholder="Type here" type="text" name="last" value="{{last}}" >

                <label for="email">Email<span class="mandatory">*</span></label>
                <input placeholder="Type here" type="email" name="email" value={{email}} >

                <label for="password">Password</label>
                <input placeholder="Type here" type="password" name="password" >

                <label for="age">Age</label>
                <input placeholder="Type here" type="number" name="age" value={{age}} >

                <label for="city">City</label>
                <input placeholder="Type here" type="text" name="city" value={{city}} >

                <label for="url">Website</label>
                <input placeholder="Type here" type="url"  pattern="http.*" name="url" value={{url}}>

                
                <p class="mandatory-message">All the fields with * are mandatory</p>
                <button class="submit-button" type="submit">SAVE CHANGES</button>
                <a href="/petition/goodbye" class="submit-button delete-button">DELETE ACCOUNT</a>
            </form>
            {{/userFullProfile}}
        </div>
        <div class="img-container">
            <img src="/images/nonna.png">
        </div>
    </div>
</div>